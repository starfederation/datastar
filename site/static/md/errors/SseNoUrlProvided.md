# Error: SseNoUrlProvided

No URL was provided to the `@{ action }()` action. A valid URL must be provided as the first argument.

Example:

```html
<button data-on-click="@{ action }('/endpoint')"></button>
```