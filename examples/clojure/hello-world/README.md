# Hello world example

## Running the example

- repl:

```clojure
clojure -M:repl -m nrepl.cmdline --middleware "[cider.nrepl/cider-middleware]"
```

- main:

```clojure

clojure -M -m example.main
```
